1. Registre suas crendencias AWS CLI
2. Configure as variaveis de ambiente `.env`
    ```.env
    USERNAME = "maxpower"
    PASSWORD = "Maxpower#12345678"
    LOCAL_SQL = "..\..\data\mysqlsampledatabase.sql"
    ```
3. `pip install -r requirements`
4. Execute os scripts na ordem